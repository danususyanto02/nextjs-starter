import { error, ok } from "@/lib/api/response"; import { externalService } from "@/lib/integrations/restful-api-dev/service"; import { ProviderError, ProviderTimeoutError } from "@/lib/integrations/http-client"; import { requireAccess } from "@/lib/rbac/guards";
function map(caught: unknown) { if (caught instanceof ProviderTimeoutError) return error("PROVIDER_TIMEOUT", "External provider timed out", 504); if (caught instanceof ProviderError) return error("PROVIDER_ERROR", "External provider request failed", 502); return error("PROVIDER_ERROR", "External provider request failed", 502); }
export async function GET(request: Request, context: { params: Promise<{ id: string }> }) { const guard = await requireAccess(request, "AM0000007"); if (guard.response) return guard.response; try { return ok(await externalService.get((await context.params).id)); } catch (caught) { return map(caught); } }
export async function PUT(request: Request, context: { params: Promise<{ id: string }> }) { const guard = await requireAccess(request, "ED0000007"); if (guard.response) return guard.response; try { return ok(await externalService.update((await context.params).id, await request.json(), "PUT")); } catch (caught) { return map(caught); } }
export async function PATCH(request: Request, context: { params: Promise<{ id: string }> }) { const guard = await requireAccess(request, "ED0000007"); if (guard.response) return guard.response; try { return ok(await externalService.update((await context.params).id, await request.json(), "PATCH")); } catch (caught) { return map(caught); } }
export async function DELETE(request: Request, context: { params: Promise<{ id: string }> }) { const guard = await requireAccess(request, "DD0000007"); if (guard.response) return guard.response; try { await externalService.remove((await context.params).id); return ok({ deleted: true }); } catch (caught) { return map(caught); } }
